<!-- <form action="/signup" method="POST">
  <input type="text" name="username" placeholder="Username" required>
  <input type="password" name="password" placeholder="Password" required>
  <button type="submit">Sign Up</button>
</form> -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SpinSterz | Signup</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet" />

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: ['"Space Grotesk"', 'sans-serif'],
            },
            colors: {
              bg: '#05060c',
              surface: '#0f1020',
              'neon-blue': '#33c9ff',
              'neon-purple': '#bc4bff',
              'text-main': '#f8fbff',
              'text-muted': '#8890b3',
              danger: '#ff6b81',
              success: '#54fcb5',
            },
            backgroundImage: {
              'card-gradient': 'linear-gradient(135deg, rgba(9, 12, 32, 0.9), rgba(8, 18, 44, 0.9))',
              'btn-gradient': 'linear-gradient(120deg, #bc4bff, #33c9ff)',
            }
          }
        }
      }
    </script>

    <style>
        /* Complex background gradients that are cleaner in custom CSS than Tailwind arbitrary values */
        body {
            background: 
                radial-gradient(circle at top, rgba(52, 24, 92, 0.25), transparent),
                radial-gradient(circle at 20% 20%, rgba(45, 119, 255, 0.2), transparent),
                #05060c; /* Fallback to bg color */
        }
        .card-glow-effect {
            background: 
                radial-gradient(circle at 20% 20%, rgba(51, 201, 255, 0.15), transparent 40%),
                radial-gradient(circle at 80% 10%, rgba(188, 75, 255, 0.2), transparent 45%),
                radial-gradient(circle at 50% 70%, rgba(84, 252, 181, 0.15), transparent 35%);
        }
        /* Custom spinner animation */
        @keyframes spin { to { transform: rotate(360deg); } }
        .animate-spin-custom { animation: spin 0.8s linear infinite; }
    </style>
  </head>
  
  <body class="min-h-screen flex justify-center items-center p-6 text-text-main">

  <div class="w-full max-w-[640px] flex flex-col gap-10">

    <header class="text-center">
      <div class="text-4xl sm:text-5xl font-bold tracking-[0.25rem] uppercase text-text-main drop-shadow-[0_0_16px_rgba(188,75,255,0.7)]">
        SpinSterz
      </div>
      <p class="mt-1 text-text-muted">Play bold. Win brighter.</p>
    </header>

    <main class="relative rounded-[28px] overflow-hidden border border-white/5 shadow-[0_35px_120px_rgba(0,0,0,0.6)] bg-card-gradient">
      <div class="card-glow-effect absolute inset-0 blur-xl z-0 pointer-events-none"></div>

      <section class="relative z-10 p-10 flex flex-col gap-6">
        <h1 class="text-3xl font-bold">Create your account</h1>
        <p class="text-text-muted">Join the neon arena.</p>

        <form method="POST" action="/signup" class="flex flex-col gap-5">

          <input type="text" name="username" placeholder="Username" required
            class="bg-[#070914cc] border border-white/10 rounded-2xl px-4 py-3.5" />

          <input type="password" name="password" placeholder="Password" required
            class="bg-[#070914cc] border border-white/10 rounded-2xl px-4 py-3.5" />

          <button type="submit"
            class="mt-2 w-full border-none rounded-[18px] py-4 font-semibold text-[#05060c] bg-btn-gradient">
            Create Account
          </button>

        </form>
      </section>
    </main>

    <footer class="text-center text-text-muted">
      Already have an account? <a href="/login" class="text-neon-blue font-semibold">Sign In</a>
    </footer>

  </div>

</body>

</html>